name: Secret Practice

on:
  workflow_dispatch:

jobs:
  secret-test:
    runs-on: ubuntu-latest

    steps:
      - name: Use secret
        env:
          PRACTICE_MESSAGE: ${{ secrets.PRACTICE_MESSAGE }}
        run: |
          echo "Secret is configured successfully."
          echo "Secret length: ${#PRACTICE_MESSAGE}"
